/**Comparison
 * 1. Bigger <
 * 2. Smaller/less >
 * 3. Equal ==
 * 4. Greater than or equal >=
 * 5.Smaller than or equal <=
 * 6. Not equal !=
 * 7. and &&
 * 8. or ||
 *  */  

/**
 * Operations
 * 1. +, -, *, /, %
 * 2. +=, -=, *=, /=
 * 3. isNaN
 * 4. null
 * 5. Number
 */

let num = 10;

num += 5;   // 15
num -= 3;   // 12
num *= 2;   // 24
// num /= 4;   // 6

console.log(num); // 6

// x += y ↔ x = x + y

// x -= y ↔ x = x - y

// x *= y ↔ x = x * y

// x /= y ↔ x = x / y

